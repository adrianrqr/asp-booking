services:
  asp-booking:
    image: asp-booking
    build: .
    env_file: .env
    environment:
      DB_PATH: /data/asp-booking.db
    volumes:
      - asp-booking-data:/data
    mem_limit: 128m
    cpus: 0.5
    restart: unless-stopped

volumes:
  asp-booking-data:
